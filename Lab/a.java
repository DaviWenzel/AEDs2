class a{

public static void mostrarRec(){
    mostrarRec(p);
    }
private void mostrarRec(int i){
    if(i!=u){
        System.out.println(a[i]);
        return mostrarRec((i + 1) % a.length());
    }
    }

public static int tam(){
    int array[] = new int[10];
    int p = 7;
    int u = 1;
    int tamanho;
    if(u < p) tamanho = p + u;
    else tamanho = u - p;
    return tamanho;
    }
}

public static boolean pesquisar(int x){
    for(int i = 0; i < n; i++){
            if(i == x) return true;
            if(u - 1 == p) return false;
        }
    }
}